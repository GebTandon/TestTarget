apiVersion: v1
kind: Service
metadata:
  name: {{ template "testtarget.fullname" . }}
  labels:
    app: {{ template "testtarget.name" . }}
    chart: {{ template "testtarget.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "testtarget.name" . }}
    release: {{ .Release.Name }}
